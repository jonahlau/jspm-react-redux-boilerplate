import { logInUserRequest, logInUserSuccess, logInUserFail, logInUser, logOut } from '../app/auth/authActions';

const actions = {
  logInUserRequest,
  logInUserSuccess,
  logInUserFail,
  logInUser,
  logOut,
};

export default actions;
